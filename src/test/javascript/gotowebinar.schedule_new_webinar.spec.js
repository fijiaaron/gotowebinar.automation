require("chai");
var should = chai.should();

describe("GotoWebinar", function() {
	describe("Schedule a new webinar", function() {
		it("should create a new webinar", function() {

			gotowebinar.create

			it("should have the given title", function() {

			}),

			it("should have the given description", function() {

			}),

			it("should be scheduled at the given time", function() {

			})
		})
	})
})


gotowebinar.login(email, password)
	navigate.to(gotowebinar.loginPage)
		verify.on(gotowebinar.loginPage)
	